using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using UnityEngine;

namespace NevernamedsItems
{
    static class GoopHooksAndExtensions
    {
	/*	public static bool IsPositionOnFire(this DeadlyDeadlyGoopManager self, Vector2 position)
		{
			IntVector2 key = (position / DeadlyDeadlyGoopManager.GOOP_GRID_SIZE).ToIntVector2(VectorConversions.Floor);
			DeadlyDeadlyGoopManager.GoopPositionData goopPositionData;
			return self.m_goopedCells.TryGetValue(key, out goopPositionData) && goopPositionData.remainingLifespan > this.goopDefinition.fadePeriod && goopPositionData.IsOnFire;
		}*/
	}
}
